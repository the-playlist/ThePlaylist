{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/pages/api/$1" },
    { "src": "/(.*)", "dest": "src/pages/$1" }
  ],
  "builds": [
    { "src": "backend/**/*.js", "use": "@vercel/node" },
    { "src": "src/**/*.js", "use": "@vercel/next" }
  ]
}
